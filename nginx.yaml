- name: Install and run nginx
  host: web
  become: yes
  tasks: 
  - name: nginx
    ansibl.builtin.yum:
      name: nginx
      state: Installed
  - name: start nginx
    ansibl.builtin.service:
      name: nginx
      state: started